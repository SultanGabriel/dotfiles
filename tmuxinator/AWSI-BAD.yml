name: AWSI-BAD
root: /mnt/s/AWSI/CloudLab/BAD_GBPsych_Chatbot/

windows:
  - editor:
      layout: main-vertical
      panes:
        - shell_command:
            - echo "[top-left pane ready]"
        - shell_command:
            - echo "[bottom-left pane ready]"
        - shell_command:
            - echo "[right pane ready]"
      # Use tmux commands to manually define layout
      commands:
        - tmux split-window -h -p 60 \; select-pane -t 0 \; split-window -v -p 60 \; select-pane -t 2
  - notes:
      root: /mnt/s/ObsidianVault/02\ -\ Areas/AWSI/CloudLab/BAD/
      panes:
        - nvim 

